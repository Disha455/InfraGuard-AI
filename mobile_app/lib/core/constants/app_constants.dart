// InfraGuard AI — Application-wide constants.
//
// Centralises values that are referenced across multiple features:
// API base URL, timeout durations, asset paths, supported image formats,
// and any other scalar values that must not be scattered as magic literals.
