# shared/widgets

Reusable, feature-agnostic UI components imported by multiple features.

Planned implementations:
- `loading_indicator.dart`   — branded circular progress indicator
- `error_banner.dart`        — standardised error/retry widget
- `image_preview.dart`       — constrained image display with placeholder
- `confidence_badge.dart`    — colour-coded confidence score chip
